let test_set = [1, 2, 3];
console.log(myset_add(test_set, 4));

function myset_add(myArray, newValue){
  for(let i = 0; i<= myArray.length; i++){
    if(myArray[i] == newValue){
      return myArray;
    }
  }
  myArray.push(newValue);
  return myArray;
  }

//create new myArray
let new_set = [3, 4, 5, 6];
function new_set_remove(myArray, newValue){
  if(newValue = (test_set.push(myArray[i]), [1, 2, 3, 4])){
    console.log(new_set);
//loop through old myArray
//if current value (myArray[i]) is the remove value, skip itfunction myset_remove(data, remove_value){
  for(let i = 0; i <= new_set.length; i++);
    if(new_set == myArray[i]){
    return new_set.remove(myArray[i]);
}else{
  return new_set.push(myArray[i]);
  }
}
//else, add the value to the new array
