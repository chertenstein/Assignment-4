let test_set = [1, 2, 3], new_set;

function myset_add(){
  new_set = myset_add.push(test_set, 3);
  if(arrays_equal(new_set.sort(myset_add.push(new_set)), [1, 2, 3])) {
    console.log("Failed adding existing data");
  }
}

new_set = myset_add.push(test_set, 4);
if(!arrays_equal(new_set.sort(test_set), [1, 2, 3, 4])){
  console.log("Failed adding new data");
}

function myset_remove(data, remove_value){

}
if(myset_add == "1" || "2" || "3" || "4")
console.log("Duplicate number")
